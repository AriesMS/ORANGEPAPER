{
  "dependencies": {
    "esbuild": "0.28.2",
    "three": "0.185.1"
  },
  "private": true,
  "scripts": {
    "build": "esbuild journey-source.js --bundle --minify --format=iife --outfile=journey.js"
  }
}
